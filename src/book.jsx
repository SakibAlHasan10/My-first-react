export default function Book({book}){
    // console.log(book)
    return (
        <div >
            <h3>Books Name: {book.name}</h3>
            <h5>Books Name: {book.price}</h5>
        </div>
    )
}